import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ablpayroll.employee',
  appName: 'ABL Payroll Solutions V1.0',
  webDir: 'dist'
};

export default config;
